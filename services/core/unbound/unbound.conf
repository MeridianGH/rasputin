server:
  interface: 0.0.0.0
  port: 53

  access-control: 0.0.0.0/0 allow

  # Performance (important for :contentReference[oaicite:1]{index=1})
  prefetch: yes
  num-threads: 2

  # Sensible caching
  cache-min-ttl: 300
  cache-max-ttl: 86400

  # DNSSEC
  auto-trust-anchor-file: "/etc/unbound/root.key"

  # Hygiene
  hide-identity: yes
  hide-version: yes
  qname-minimisation: yes

  verbosity: 3
  log-queries: yes
  log-replies: yes
