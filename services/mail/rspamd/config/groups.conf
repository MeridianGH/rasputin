symbols = {
  "FUZZY_DENIED" {
    weight = 4.5;
  }
  "BAYES_SPAM" {
    weight = 6.0;
  }
  "BAYES_HAM" {
    weight = -4.0;
  }

  # --- Reply-To (critical fix) ---
  HAS_REPLYTO {
    weight = 0.8;
  }

  REPLYTO_DOM_NEQ_FROM_DOM {
    weight = 2;
  }

  REPLYTO_DOM_NEQ_TO_DOM {
    weight = 1;
  }

  # --- Structural bulk spam signals ---
  RCPT_COUNT_ONE {
    weight = 0.7;
  }

  MISSING_XM_UA {
    weight = 0.8;
  }

  TO_DN_NONE {
    weight = 0.6;
  }

  MIME_HTML_ONLY {
    weight = 0.6;
  }

  # --- Formatting / anomaly signals ---
  URI_COUNT_ODD {
    weight = 1.5;
  }

  R_NO_SPACE_IN_FROM {
    weight = 1.5;
  }

  FROM_EXCESS_QP {
    weight = 1.5;
  }

  MANY_INVISIBLE_PARTS {
    weight = 1.5;
  }

  # --- Reduce over-trust ---
  MIME_GOOD {
    weight = 0.0;
  }
}
